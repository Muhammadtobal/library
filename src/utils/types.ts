export enum UserRole {
  ADMIN = 'admin',
  User = 'user',
  // MEMBER = 'member',
}
export enum DisplayType {
  WITHOUT_DOWNLOADING_OR_READING = 'Without downloading or reading',
  TO_DOWNLOAD_AND_READ = 'To download and read',
  FOR_READING_ONLY = 'For reading only',
}
